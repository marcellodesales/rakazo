apiVersion: v1
kind: Service
metadata:
  name: web
  labels:
    app.kubernetes.io/name: web
    app.kubernetes.io/part-of: rakazo
spec:
  selector:
    app.kubernetes.io/name: web
  ports:
    - name: http
      port: 5173
      targetPort: http
