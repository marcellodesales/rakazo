apiVersion: v1
kind: Service
metadata:
  name: api
  labels:
    app.kubernetes.io/name: api
    app.kubernetes.io/part-of: rakazo
spec:
  selector:
    app.kubernetes.io/name: api
  ports:
    - name: http
      port: 3100
      targetPort: http
