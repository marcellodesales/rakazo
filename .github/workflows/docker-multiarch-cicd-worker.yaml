name: "🐳 docker-multiarch-cicd-worker"
run-name: "🐳 docker-multiarch-cicd-worker · ${{ github.event_name }} · ${{ github.ref_name }}"
on:
  pull_request:
    types: [opened, synchronize, reopened, closed]
  push:
    tags: ['v*']

concurrency:
  group: docker-multiarch-cicd-worker-caller-${{ github.ref }}
  cancel-in-progress: false

jobs:
  docker-multiarch:
    uses: ohanalabs-ai/github-platform/.github/workflows/docker-multiarch-cicd.yaml@main
    permissions:
      contents: write
      packages: write
      id-token: write
      attestations: write
      artifact-metadata: write
      pull-requests: write
    with:
      registry: ghcr.io
      platforms: linux/amd64,linux/arm64
      docker-compose-file-name: infra/compose/docker-compose.cicd.yml
      docker-compose-target-service: worker
      push-attestations: 'false'
